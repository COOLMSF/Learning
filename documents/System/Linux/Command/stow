Function:delete all related files that installed from sorce

Example:
wget http://ftp.gnu.org/gnu/hello/hello-2.10.tar.gz

tar -zxvf hello-2.10.tar.gz

cd hello-2.10/

./configure --prefix=/usr/local/stow/hello //This step is to configure and make path

make
sudo make install

ls /usr/local/stow/hello/ //There are two files here

cd /usr/local/stow
sudo stow hello //Generate symbolic link


